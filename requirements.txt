requests>=2.31.0
python-dateutil>=2.8.2
yfinance>=0.2.54
pandas>=2.2.0
exchange-calendars>=4.5.0
fastapi>=0.100.0
uvicorn>=0.23.0
python-dotenv>=1.0.0
jinja2>=3.1.2
pydantic-settings>=2.0.0
loguru>=0.7.0
tenacity>=8.0.0
python-multipart>=0.0.9
faster-whisper>=1.0.0
imageio-ffmpeg>=0.5.1
soundcard>=0.4.6

# Optional: enables Telegram signal collection for the futures signal dashboard.
telethon>=1.34.0

# LLM signal extraction (Claude API)
anthropic>=0.20.0

# Bybit trading API
pybit>=5.0.0

# Process monitoring (used for poll.py running state check)
psutil>=5.9.0

# RL Model dependencies (Uncomment to use RL features on Dashboard or Training)
# stable-baselines3>=2.3.0
# gymnasium>=0.29.1
# psycopg2-binary  # removed: project uses SQLite only; causes install failures on Windows
lxml>=4.9.0
